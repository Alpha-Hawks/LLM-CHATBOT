# Core Web & API
fastapi>=0.110.0
uvicorn[standard]>=0.28.0
pydantic>=2.6.0
pydantic-settings>=2.2.0
httpx>=0.27.0
openai>=1.20.0
beautifulsoup4>=4.12.3
cryptography>=42.0.5
PyJWT[crypto]>=2.8.0
python-multipart>=0.0.9
requests>=2.31.0

# Database & Async Drivers
sqlalchemy>=2.0.28
aiosqlite>=0.20.0
asyncpg>=0.29.0

# Note: Heavy training dependencies (torch, transformers, peft, trl, datasets, accelerate)
# are moved to training/requirements.txt so cloud deployments (Vercel, Render) remain fast and lightweight.
